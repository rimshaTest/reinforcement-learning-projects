EECE 5614: Reinforcement Learning
Project1

This assignment covers Epsilon-greedy, Gradient Bandit, and Upper Confidence Bound (UCB) strategies for multi-armed bandit.

To run the solution to this, run the following command:

```
pip install matplotlib && python .\project1code.py
```